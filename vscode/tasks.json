{
  "version": "2.0.0",
  "tasks": [
    // 選択したファイルのパスと行番号をクリップボードにコピーする
    {
      "label": "CopyPathWithLineNumber",
      "type": "shell",
      "command": "printf '%s' '${file}:${lineNumber}' | iconv -f UTF-8 -t UTF-16LE | clip.exe",
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "close": true
      }
    }
  ]
}
